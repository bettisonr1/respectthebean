{
  "name": "coffee-tracker-local",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "dev:local:infra": "docker compose up -d dynamodb",
    "db:create-table": "aws dynamodb create-table --table-name coffee-tracker --attribute-definitions AttributeName=PK,AttributeType=S AttributeName=SK,AttributeType=S AttributeName=communityPK,AttributeType=S AttributeName=communitySK,AttributeType=S --key-schema AttributeName=PK,KeyType=HASH AttributeName=SK,KeyType=RANGE --global-secondary-indexes 'IndexName=gsi-community,KeySchema=[{AttributeName=communityPK,KeyType=HASH},{AttributeName=communitySK,KeyType=RANGE}],Projection={ProjectionType=INCLUDE,NonKeyAttributes=[\"grindSetting\",\"doseIn\",\"yieldOut\",\"extractionTime\",\"rating\"]}' --billing-mode PAY_PER_REQUEST --endpoint-url http://localhost:8000 --region eu-west-1",
    "dev:local": "npm run dev:local:infra && concurrently -k -n BACKEND,FRONTEND \"cd backend && AWS_REGION=eu-west-1 DYNAMODB_ENDPOINT=http://localhost:8000 DYNAMODB_TABLE=coffee-tracker FRONTEND_URL=http://localhost:5173 npm run dev\" \"cd frontend && npm run dev\"",
    "dev:local:down": "docker compose down"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
